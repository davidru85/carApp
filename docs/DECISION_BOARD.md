# Decision Board - carApp MVP

> **Sole registry of decision IDs (`D-n`)** and authoritative for which libraries, services and technical options are allowed. Accepted decisions MUST be reflected in an ADR and mirrored in `docs/SPECIFICATION.md §12` and `docs/TECHNICAL_PLAN.md §2`; `contract-check` asserts that mirrored decision IDs and statuses stay aligned. See `AGENTS.md` for authority and normative language.

## Scope of this registry

The registry is not limited to decisions taken during the definition phase. **Any decision taken
while implementing a story — a build-model choice, an identifier convention, a policy such as how
`targetSdk` is pinned — MUST be added here with a new decision ID and MUST get its own ADR**, in the
same pull request that makes the decision. A decision recorded only in a handoff, a commit message
or `docs/PROJECT_LOG.md` is not recorded: those are history, not authority, and the next agent has
no obligation to follow them.

## Decision Status

One vocabulary, shared with the ADR `Status` field. An ADR recording a deferral has `Status: Deferred`, not `Accepted`.

- `Proposed`: a recommendation is on the table and requires owner confirmation before the first story that depends on it starts.
- `Accepted`: selected for the MVP.
- `Deferred`: intentionally not part of the MVP but reserved for future use.
- `Pending`: no recommendation yet; requires an owner decision.
- `Rejected`: explicitly not allowed for the MVP.
- `Superseded`: replaced by a later decision, which MUST be named.

**Blocking rule:** a `Proposed` or `Pending` decision that any Ready story depends on is itself a blocking backlog item. A story that needs it is not Ready (`AGENTS.md`, Definition of Ready).

## Core Stack Decisions

| ID | Area | Choice | Alternatives Reviewed | Status | Guardrail |
|----|------|--------|-----------------------|--------|-----------|
| D-0 | Remote backend | Cloud Firestore | Supabase, AWS backend, custom API, local-only MVP | Accepted | Firestore is a backup and recovery replica behind `RemoteSyncSource`, never UI source of truth. |
| D-1 | Local database | Room 3 KMP | SQLDelight, Realm, DataStore-only | Superseded | Superseded by `D-36` after Room could not express the mandatory SQLite `CHECK` constraints as one generated schema. |
| D-2 | iOS interop | SKIE | Raw KMP export, manual wrappers | Superseded | D-58 retains SKIE but moves framework generation and SKIE processing to `:composition:ios`. |
| D-3 | Dependency injection | Koin KMP | Manual DI, Metro, Kotlin Inject | Accepted | Koin is wiring only. No service locator access in domain, use cases or repositories. |
| D-4 | `fuelType` | Store MVP combustion/fuel-like values on `Vehicle` from day one | Add later, store per fuel entry, include electric/hybrid in MVP | Accepted | Persist default `GASOLINE`; metadata only; `ELECTRIC` and `HYBRID` are deferred to a future energy model (`E5-01`). The original "no MVP UI selector" clause is superseded by `D-127`; the storage decision is unchanged. |
| D-5 | Firestore access | Firebase Firestore behind `RemoteSyncSource` | Ktor + Firestore REST, native SDK wrappers, custom API | Accepted | Firebase used directly behind integration boundaries. |
| D-6 | Authentication | Firebase Auth behind `AuthClient` | Custom auth, native-only wrappers | Accepted | Auth provider types never cross `:integration:firebase-auth`. |
| D-7 | Navigation | Native per platform | Shared router, Compose Multiplatform navigation | Accepted | No shared destination sealed class. |
| D-8 | Presentation | Shared KMP state holders | Native-only ViewModels, shared UI | Accepted | State holders expose `StateFlow<UiState>` and intents; `UiState` carries no user-facing text. |
| D-9 | Firestore offline persistence | Disabled | Enabled Firestore SDK cache | Accepted | The SQLDelight local database and the outbox are the only offline model. |
| D-10 | Metrics | Firebase Analytics behind `AnalyticsTracker` | PostHog, custom metrics, none | Accepted | Closed `AnalyticsEvent` hierarchy; no analytics calls from domain or data; off by default. |
| D-11 | HTTP/API client | Ktor, for future API-based remote implementations | Retrofit, native URLSession/OkHttp wrappers, no HTTP abstraction | Deferred | Do not add Ktor to the MVP. |
| D-12 | Image loading | Coil, if image loading ever becomes necessary | SDWebImage, platform-native loaders, none | Deferred | No image loading dependency until a story requires it; Coil is then the only approved library. |
| D-13 | Firestore location | `europe-west1` single region | `eur3` European multi-region, `nam5` United States multi-region | Accepted | Firestore is a backup and recovery replica only; the local database is the source of truth. The location is immutable after database creation and must be verified before `E0-07` creates the database. |
| D-14 | Firebase project topology | One development Firebase project plus the local emulator now; add a separate production Firebase project before release | Two projects from day one, single project through production, three projects with staging | Accepted | Development uses one real Firebase project for manual testing, CI uses the emulator only, and no public release build may point at the development project. |
| D-15 | Logging implementation | Kermit behind `Logger` | Napier, custom sinks, no implementation | Accepted | The `Logger` abstraction is mandatory regardless; Kermit never appears outside the sink implementation. |
| D-16 | Architecture checks | Konsist for package-level rules, custom Gradle check for module-level rules | Custom checks only, dependency-analysis plugin | Accepted | Gradle cannot express intra-module package rules, and features are one module each. Every rule needs a failing fixture test. |
| D-17 | Flow testing helper | Turbine | Manual collection | Accepted | Confirm compatibility during version pinning in `E0-06`. |
| D-18 | Coverage measurement | Kover with per-module thresholds | No measurement, JaCoCo | Accepted | `:core:model` and `:core:common` at least 90%, feature `domain` 85%, `:core:sync` 80%, enforced in CI. |
| D-19 | Result type | Custom `Outcome<T, E>` in `:core:common` | `kotlin.Result`, Arrow `Either`, exceptions | Accepted | `kotlin.Result` has a single type parameter. Arrow is rejected for MVP dependency surface. Declared in `docs/CONTRACTS.md §20.1`. |
| D-20 | Localization implementation | Native Android and iOS resources | Shared resource library (moko-resources) | Accepted | UI is native. `UiState` carries typed values only, so shared code never needs a string bundle. |
| D-21 | Crash reporting | Firebase Crashlytics behind `CrashReporter` | Sentry, none | Accepted | Added in Phase 4. Crashlytics types never leave `:integration:firebase-crashlytics` or `:wiring:firebase`. |
| D-22 | Application identifiers | Fixed in `docs/identifiers.md` | — | Accepted | Agents MUST NOT invent an applicationId, bundle id, namespace, project name or region. The production Firebase project ID is deferred by `D-14`. |
| D-23 | Account deletion execution | Firebase Admin server operation | Client hard-delete exception, tombstone-only purge, manual support deletion | Accepted | Client Firestore rules keep `allow delete: if false`. Account deletion hard deletes run only in an authenticated server/Admin environment that verifies the caller UID, deletes `fuelEntries`, then `vehicles`, then the Firebase Auth user, and returns success before the app clears local data. |
| D-24 | Module Android namespaces | Derived from the Gradle module path | One concrete value per module, per-module free choice | Accepted | Namespace = shared package root + module path, `:`→`.`, `-` removed. Not the Kotlin package root. `:androidApp` keeps `com.ruizurraca.carapp`. |
| D-25 | `targetSdk` policy | Pinned independently of `compileSdk`; `compileSdk 37`, `targetSdk 36` | Keep both at the same level | Accepted | `compileSdk` is forced by the Compose BOM; `targetSdk` is a runtime opt-in and a behavioural change. `E4-04` owns raising it before release. |
| D-26 | Monetary golden values | Correct the contradictory golden row and add a real HALF_UP round-up row | Keep the row and add a minimum-one-minor-unit rule; correct the row only | Accepted | Golden row 3 contradicted the formula it illustrates. The formula is authoritative; both rounding directions stay covered. |
| D-27 | `testAppGraphDependencies` ownership | Built by `E0-07`, not `E0-03` | Create stub `:core:*` modules in Phase 0; build a partial factory | Accepted | Four of the 15 `AppGraphDependencies` members have types owned by modules Phase 0 forbids creating. |
| D-28 | Feature-layer package rules | Implemented with Konsist in `E1-07` | A dedicated architecture-test module now; extend the custom checker and drop Konsist | Accepted | Konsist rules need a module to live in and no `:feature:*` module exists. The 14 module-level rows are enforced by `E0-04`. |
| D-29 | Contract type declarations | A type may be declared in `§20` or inline in the section that owns it | Move all declarations into `§20`; leave the assertion unenforceable | Accepted | Nine types are declared inline beside the prose that constrains them. `§18` assertion 1 is worded to accept either location. |
| D-30 | Walking skeleton position | `E0-07` is the second story of Phase 1, after `E1-01` | Allow `E0-07` to create `:core:database`; drop local persistence from `E0-07` | Accepted | `E0-07` needs the local database, which lives in `:core:database`, a module Phase 0 forbade and `E0-04` enforced. |
| D-31 | Branch protection | `main` requires all nine CI checks of `§18` | Require only the seven checks that verify something today | Accepted | Administrator enforcement is off so the owner can land pull requests that predate the workflow. |
| D-32 | Development Firebase project ID | `davidruiz-carapp-dev` | `carapp-dev` (unavailable) | Accepted | `carapp-dev` is held by another Google Cloud customer: `409 ALREADY_EXISTS` on create and `403 PERMISSION_DENIED` on addFirebase. Application identifiers are unchanged. |
| D-33 | Repository visibility and branch protection | Stay private; apply the `D-31` protection in the same change that makes the repository public or moves it to a plan that allows it | Upgrade to GitHub Pro now; make the repository public now | Superseded | Branch protection is gated for a private repository on the Free plan. Until the trigger fires, CI reports but does not gate, and a merge on red is possible. |
| D-34 | Repository visibility and branch protection | Repository is public; the `D-31` branch protection is active | Upgrade to GitHub Pro; stay private and reduce CI | Accepted | Supersedes `D-33`. Public repositories get free standard runners and can use branch protection; the macOS jobs cost ten times wall-clock and the account had exhausted its minutes. |
| D-35 | CI job topology | `shared-tests` and `ios-simulator-build` stay separate jobs | Merge them into one macOS job | Accepted | They are complementary diagnostics — shared-logic behaviour versus framework linking — and a single job cannot report both independently. They also run in parallel, so separating them costs no wall-clock. |
| D-36 | Local database implementation | SQLDelight 2.3.2 with AndroidX bundled SQLite 2.7.0 through `sqldelight-androidx-driver` 0.2.1 | Room 3 KMP with callback-owned DDL; SQLDelight official platform drivers; raise `minSdk` to 30 | Accepted | `.sq` files are the canonical schema, generated operations are asynchronous, the SQLite 3.24 dialect preserves the exact outbox UPSERT, and the bundled engine keeps identical SQLite behaviour on Android and iOS with `minSdk 26`. |
| D-37 | Kotlin/Native iOS targets | Support `iosArm64` and `iosSimulatorArm64`; remove `iosX64` | Use the official SQLDelight Native driver only for `iosX64`; use it for every iOS target; maintain private `iosX64` forks | Accepted | The application and CI already build ARM64 only, while the `D-36` AndroidX bundled SQLite stack publishes no `iosX64` variants. Reintroducing Intel-simulator support requires a compatible complete dependency set and a superseding decision. |
| D-38 | Database-owned mutation strategy | Use a Kotlin/SQLDelight `DatabaseMutations` transaction facade; forbid direct generated entity-mutation calls outside `:core:database` | SQLite triggers; split Kotlin/trigger implementation | Accepted | The facade can capture pre-write state, apply the exact de-duplicated recompute set, notify SQLDelight observers and keep every change in one transaction. Triggers obscure recomputation and do not reliably expose indirect table changes to SQLDelight `Flow`. |
| D-39 | Walking-skeleton data slice | Use a minimal valid `Vehicle` slice; the edited proof value is the vehicle name | Full vehicle form and repository; fuel entry with a seeded vehicle | Accepted | The closed Firestore schema permits no temporary collection. E0-07 proves a real vehicle round trip without absorbing E1-02 and E1-03. |
| D-40 | Firestore-rule sequencing | Complete `E3-01` before `E0-07` | Fold E3-01 into E0-07; deploy temporary cloud-only rules | Accepted | The first real client write must use the complete reviewed rule set and emulator evidence, never temporary permissive rules. |
| D-41 | Development Android Firebase certificate | Restrict the debug Firebase key to `com.ruizurraca.carapp.debug` and the owner's current local debug signing certificate | Dedicated development keystore; defer restriction | Accepted | The repository is public. A new development machine requires an explicitly registered fingerprint; keystores and fingerprints stay out of repository documentation. |
| D-42 | Provider-proof sequencing | Complete `E3-06` before `E3-01`, which remains before `E0-07` | Fold E3-06 into E3-01; relax the placeholder check | Accepted | The required provider-decoupling check becomes executable before the first integration module appears. |
| D-43 | Provider-exclusion control | Use `carapp.excludeFirebaseProviders=true` in the canonical Gradle settings | Separate settings file; generated CI-only settings | Accepted | Local and CI verification use the same module graph definition; a second provider-free settings file is forbidden. |
| D-44 | Provider-module registry | Explicit canonical provider paths, included only when their directories exist and exclusion is off | Filesystem discovery; per-story ad-hoc conditions | Accepted | The proof is testable before provider modules exist, planned paths do not create empty projects and unknown modules are not silently admitted. |
| D-45 | Provider-decoupling platforms | Run Android host and `iosSimulatorArm64` provider-free tests in one macOS job | Android host only on Ubuntu; separate platform jobs | Accepted | Both JVM and Kotlin/Native dependency surfaces are checked while the required branch-protection name stays `provider-decoupling`. |
| D-46 | Firestore rules test stack | Node 22.22.3, Firebase CLI 15.28.1, Firebase JS 12.18.0, `@firebase/rules-unit-testing` 5.0.1 and built-in `node:test`, pinned by npm manifests | Direct REST harness; Kotlin/GitLive integration tests; third-party JS test runner | Accepted | Use the official auth-mocking emulator library without adding Jest, Mocha or Vitest. Gradle versions remain in the version catalog; Node-only versions live in `package.json` and `package-lock.json`. |
| D-47 | Firestore rules CI placement | Run emulator tests as a named step of the protected `contract-check` job | Add a tenth protected job; run under provider decoupling | Accepted | The rules are executable contract evidence and remain mandatory without changing the nine protected check names. |
| D-48 | Firestore client-cache configuration ownership | E0-07 owns the executable disabled-persistence configuration; E3-01 owns only rules, indexes and emulator tests | Create `:integration:firebase-firestore` in E3-01 | Accepted | E0-07 already configures the first real client; E3-01 must not introduce a premature provider module or native Firebase linking. |
| D-49 | MVP Firestore schema-version rule | Accept exactly `schemaVersion == 1` during the MVP | Accept every version `>= 1`; accept a predeclared version range | Accepted | Matches the closed remote schema, `CLIENT_MAX_SCHEMA_VERSION` and E3-01 acceptance criteria; a future schema rollout requires an explicit sequencing decision. |
| D-50 | Firestore first-page cursor | Use `startAt(overlapSince)` for the first page and the full timestamp/document-ID cursor for later pages | Rely only on the range filter; use a minimum UUID sentinel | Accepted | Firebase JS 12.18.0 rejects an empty document-ID cursor; a timestamp-only first boundary includes every document at the overlap timestamp without excluding malformed IDs that must be quarantined. |
| D-51 | npm install-script policy | Disable dependency install scripts repository-wide with `.npmrc` | Approve pinned scripts and pin npm 12; leave behavior to the installed npm version | Accepted | The emulator suite passes without the four blocked transitive scripts; a repository policy is deterministic across npm versions and reduces supply-chain execution. |
| D-52 | Firebase CLI audit residual | Retain Firebase CLI 15.28.1 and document its moderate dev-tool-only transitive advisories | Force-downgrade the CLI; override unsupported transitive majors | Accepted | The affected Pub/Sub baggage and UUID buffer paths are not used by the emulator harness and no package ships in the app; forced remediation would contradict D-46 or exceed upstream compatibility. |
| D-53 | Development Firebase app provisioning | Register separate Android and iOS debug apps, use their Firebase-provisioned platform keys, restrict each key before committing its configuration | Pre-create custom restricted keys; share one client key | Accepted | Google permits only one application-restriction type per key; separate platform keys keep the public debug configurations isolated and follow the official Firebase provisioning path. |
| D-54 | Development Firebase configuration isolation | Keep Firebase configuration debug-only on both platforms and fail release builds closed until production configuration exists | Share development configuration with release builds; inject untracked local files | Accepted | Build-time isolation prevents a release from reaching the development backend, while committed restricted debug configuration keeps local and CI setup reproducible. |
| D-55 | Walking-skeleton staged ownership | E0-07 creates final modules and public contracts, implements only the real Vehicle slice and leaves deterministic unwired shells for the remaining exported state holders | Narrow the E0-07 ABI temporarily; move E0-07 after the owning feature/auth/sync stories | Accepted | Preserves the full Swift golden-header gate and final module topology without absorbing later product behaviors or introducing temporary public API. |
| D-56 | Test app graph factory module | Place `testAppGraphDependencies(...)` in `:shared:testing`; keep `AppGraphDependencies` in `:shared` and `:core:testing` generic | Allow `:core:testing` to depend on `:shared`; move `AppGraphDependencies` to a new central module | Accepted | A dedicated KMP test-support module preserves the application-to-core dependency direction while making the factory reusable from `commonTest`. |
| D-57 | Android Firebase configuration plugin | Pin Google Services Gradle plugin 4.5.0 | Retain the previous stable 4.4.4 | Accepted | Use the current stable version documented by Firebase to process the debug-only `google-services.json`; the version lives only in the Gradle version catalog. |
| D-58 | iOS framework composition ownership | `:composition:ios` produces the single `Shared` framework, exports `:shared` and composes `:wiring:firebase` | Let `:shared` depend on wiring; use two frameworks with prior registration | Accepted | A dedicated composition root avoids the Gradle cycle, preserves provider-free `:shared` tests and prevents duplicated Kotlin/Native runtimes and global registration. Supersedes D-2's module-ownership constraint while retaining SKIE. |
| D-59 | `AppProviders` port shape | Explicit typed properties for every `AppGraphDependencies` dependency except `isDebugBuild` | Return the complete dependency container from one method; expose only Firebase providers | Accepted | The explicit port keeps graph construction compile-time checked, provider-free and free of service-location semantics; `buildAppGraph` remains responsible for applying `isDebugBuild`. |
| D-60 | Anonymous identity retention and portability | Treat an unlinked anonymous identity as device-bound and enable Firebase's native 30-day automatic cleanup | Promise cross-device anonymous recovery; retain anonymous accounts indefinitely | Accepted | Only a linked permanent provider makes the account recoverable on another device; associated data cleanup is owned by D-63. |
| D-61 | Account-linking collision precedence | Keep the current anonymous-session snapshot and replace the pre-existing permanent-account data after explicit confirmation | Keep the permanent account; automatically merge both accounts | Accepted | The destructive flow is resumable, captures proof of anonymous ownership before switching and leaves the anonymous session untouched on cancellation. |
| D-62 | Anonymous sign-in benefit reminders | Fixed day-1, day-3, day-8 and day-18 timeline with highest-due reminder collapse | Recalculate from display time; show every missed reminder | Accepted | The schedule is anchored to Firebase account creation, is non-blocking and completes permanently after reminder 4 or permanent sign-in. |
| D-63 | User-data cleanup implementation | Repository-owned idempotent service, direct collision-path invocation and one temporary 1st gen Auth deletion trigger | Firebase Delete User Data extension; rely only on the trigger | Accepted | Firebase Extensions management sunsets on 2027-03-31; `onAnonymousUserDeleted` is the only permitted 1st gen function and its migration is tracked as TD-01. |
| D-64 | Anonymous lifecycle delivery | Split retention, conversion, reminders, backend cleanup and cross-device proof across their owning stories | Implement the complete lifecycle in E0-07 | Accepted | Keeps E0-07 reviewable and moves cross-device evidence to the first point where permanent auth and complete sync coexist. |
| D-65 | Firebase Apple SDK compatibility pin | Pin Firebase Apple SDK 11.8.0 exactly with GitLive 2.6.0 | Use Firebase Apple 12.18.0; use Firebase Apple 11.15.0 | Accepted | GitLive 2.6.0 Apple cinterop bindings are generated against 11.8.0; using a different native SDK can compile but fail at runtime. The two pins move together when GitLive publishes bindings for a supported newer Firebase Apple SDK. |
| D-66 | Development cloud cost containment | EUR 10 monthly alerts-only budget, actual-cost alerts at 50/90/100%, project-local 2nd gen cutoff at 100%, Node.js 22 and read-only GitHub OIDC verification | Cut off at 90%; forecast cutoff; separate FinOps project; Python; Node.js 24 split or downgrade | Accepted | The budget is notification-only and delayed; the development project deliberately stops when the cutoff fires. Production uses aggressive alerts and manual intervention, never automatic billing shutdown. Node.js 22 is coupled to D-63 and moves under TD-01. |
| D-67 | Firebase App Check enforcement | Enforce Authentication and Firestore with App Attest, Play Integrity and build-restricted debug providers | Keep App Check out of MVP; integrate without enforcement | Accepted | Billing turns anonymous-authentication abuse from console noise into a direct cost vector. App Check supplements, never replaces, Authentication and Firestore Rules. |
| D-68 | Cloud Functions moderate advisory residual | Retain the official SDK graph while GHSA-w5hq-g745-h8pq remains dynamically unreachable from `stopBilling` | Replace Firebase Functions with Functions Framework; force transitive overrides or SDK downgrades | Accepted | `firebase-admin` is a mandatory `firebase-functions` peer, not a removable future-only dependency. The production audit residual expires into the TD-01 quarterly review and CI still blocks high and critical findings. |
| D-69 | Billing cutoff account privilege | Grant the dedicated keyless cutoff identity `roles/billing.admin` with an administrative-change alert | Leave billing disabled; create a Google Cloud organization solely for one custom billing role | Accepted | A personal billing account cannot host a custom role. The broad standard role is isolated from product functions and its use is visible at the owner's alert address. |
| D-70 | Billing cutoff delivery policy | Disable Pub/Sub retries, alert on every execution error and rely only on the observed recurring budget publication after a dropped event | Retry failures for up to seven days | Accepted | `maxInstances = 1` bounds concurrency, not retry duration. A persistent failure must signal once rather than become recurring billable work. |
| D-71 | iOS simulator signing | Let Xcode perform normal simulator signing without committing a developer team or account-specific credential | Command-line-only signing override; unsigned cleanup hook | Accepted | The E0-01 unsigned scaffold rule protected a project with no Apple services, but now prevents Keychain-backed Firebase Auth persistence and makes simulator acceptance unrepresentative of production. |
| D-72 | Billing cutoff acceptance trigger | Publish two controlled schema-valid threshold events to the real budget topic after independently measuring real budget publications | Deliberately incur billable usage; wait for organic spend | Accepted | Exercises the deployed Pub/Sub, Eventarc, function and Billing API path deterministically without spending money, records expected versus actual impact and proves the already-disabled no-op. |
| D-73 | Cloud Functions artifact retention | Apply a one-day cleanup policy to the exact Artifact Registry repository used by Cloud Functions and observe an eligible deletion | Seven-day retention; no cleanup | Accepted | Git is the source of truth; one day bounds development storage cost while preserving a brief inspection window. |
| D-74 | iOS Keychain persistence acceptance | Dedicated XCUITest target `com.ruizurraca.carapp.uitests` drives the real app UI across `terminate()` and `launch()` | Manual simulator acceptance; Debug host launch hook | Accepted | Only a separate UI-test process observing the signed app after a real restart proves Firebase Auth persisted through the app's Keychain without contaminating the shipping host. |
| D-75 | Firebase standalone Kotlin/Native test exception | Derive the exemption from the transitive project graph rooted at the two Firebase integration modules; its current resolution is those modules plus `:wiring:firebase` and `:composition:ios` | Split pure and concrete wiring; add CocoaPods beside SwiftPM; adopt experimental SwiftPM import on beta Kotlin; maintain test-only Firebase XCFrameworks | Accepted | GitLive does not link Apple SDK dependencies transitively into standalone Kotlin/Native test binaries. CI compares the derived and declared sets in both directions, and TD-01 reviews the unchanged upstream expiry signals. |
| D-76 | Vehicle validation boundary | Pure Vehicle validators receive immutable pre-write facts; E1-03 is the imperative shell that loads facts, validates and mutates in one transaction | CRUD use cases with additional repository query ports; validation only inside the data repository | Accepted | Extends the contractual `CalculateConsumption` functional-core pattern to writes; the transaction is the only local guarantee because remote duplicates must remain ingestible without a unique index. |
| D-77 | Fuel Entry validation boundary | Pure Fuel Entry validators receive immutable pre-write facts and return canonical persistence values; E1-06 loads facts, validates and mutates in one transaction | CRUD orchestrators with validation query ports; validation only inside the data repository | Accepted | Mirrors D-76 for state-dependent R-1, keeps R-2 derivation pure and makes an unconfirmed warning incapable of mutating persistence. |
| D-78 | Consumption invalidation precedence | `NoPreviousFullTank > NonPositiveDistance > DuplicateOdometerInSegment > MissedEntriesInSegment > InconsistentOdometerInSegment` | Prioritize user-entered flags; return every applicable reason | Accepted | The unconditional non-positive-distance contract must win before any flag, while a stable single-reason precedence preserves the canonical model and Swift projection. |
| D-79 | Consumption repository-filter evidence | Move the repository-filter acceptance criterion from E1-05 to E1-06; E1-05 proves `CalculateConsumption` does not filter its input | Pull repository work into E1-05; test a fake repository | Accepted | The production filter belongs to the E1-06 repository implementation; a fake would prove only itself and pulling the repository forward would cross the story and database boundaries. |
| D-80 | Consumption performance evidence | Use a standalone uninstrumented JVM benchmark task and an optimized iOS device-test binary over one shared 1,000-entry dataset; review the measured JVM margin before enabling a CI threshold gate | Measure under normal instrumented tests; defer all release evidence to E4-03 | Accepted | Kover distorts wall-clock evidence, while the mandatory real-device result must remain explicitly pending when no iPhone is available. |
| D-81 | Fuel Entry earliest-date calendar | Subtract 20 calendar years from `vehicle.createdAt` with `Instant.minus(20, DateTimeUnit.YEAR, TimeZone.UTC)`, then clamp the result to the Unix epoch | Fixed duration of 7,305 days; fixed duration of 7,300 days | Accepted | Literal calendar years preserve the behavioral rule through the accepted commonMain library; fixed durations either change the rule for an implementation shortcut or accept forbidden dates. |
| D-82 | Fuel Entry odometer inconsistency derivation | Flag an active row when its odometer is at or below its previous active chronological neighbour or strictly below its Vehicle initial odometer; a missing Vehicle disables only the initial-odometer branch | Restrict persistence to the neighbour branch; write the confirmed warning from `:feature:fuel` | Accepted | Matches the validation predicate and preserves the confirmed warning in local and outbox snapshots while keeping `:core:database` as the sole writer and orphan rows safe. |
| D-83 | Fuel Entry bounded projection window | Select the highest 5,000 rows in each canonical projection ordering through a descending inner limit, then return that window in canonical ascending order | Keep the lowest/oldest rows; remove the in-memory cap | Accepted | New Fuel Entries must remain visible and consumption must continue moving after the cap without changing deterministic §4 result ordering or the production memory ceiling. |
| D-84 | Android Vehicle UI support stack | Compose Navigation 2.9.8, BOM-managed Compose UI tests, AndroidX Test runner/rules 1.7.0 and a SHA-pinned emulator CI job | Navigation 3; manual navigation with Robolectric | Accepted | Implements D-7 with stable AndroidX releases and runs the mandatory Vehicle creation test against the real Android host. |
| D-85 | Vehicle presentation ownership | Move Vehicle presentation to `:feature:vehicle`; move `UiMessage`, `UiMessageKind` and `SyncStatus` to `:core:common` | New `:core:presentation`; retain presentation in `:shared` | Accepted | Keeps the feature presentation boundary strict while preserving the Swift ABI through exact Objective-C names and explicit framework exports. |
| D-86 | Kotlin and Swift graph separation | `buildAppGraph` returns hidden `AppGraph`; `SwiftAppGraph` wraps it by composition | Separate Android factory; make `SwiftAppGraph` implement `AppGraph` | Accepted | Gives Android caller-owned scopes without leaking `CoroutineScope`, `SyncController` or `AppGraph` into Objective-C. |
| D-87 | Vehicle editability projection | Observe reactive `VehicleEditFacts` from Vehicle and active Fuel Entry changes | One-shot suspend query; optimistic edit with save-time rejection only | Accepted | The form converges after background recovery while transactional validation remains authoritative. |
| D-88 | Pre-E3-03 sync-status staging | Keep D-55 direct Vehicle restoration and constant `SyncStatus.Idle`; do not create a provisional controller | Provisional controller; pull E3-03 forward | Accepted | Avoids a second sync source of truth; E3-03 closes the explicit temporary exception. |
| D-89 | Local database lifetime ownership | `DatabaseFactory.create()` returns a `DatabaseHandle` that owns one `AppDatabase` and its `SqlDriver` | Add `DatabaseFactory.close(database)`; expose the driver to `:shared` | Accepted | Keeps the closeable resource in `:core:database`; `AppGraph.close()` releases its handle exactly once and `SwiftAppGraph.close()` does so transitively without changing the Swift ABI. |
| D-90 | Swift holder release and Vehicle creation completion | Add keyed release functions to `SwiftAppGraph`; expose `savedVehicleId` separately and reset creation inputs after success | Rekey a creation holder as an editor; drop holder caching | Accepted | Prevents a cached creation holder from overwriting its first Vehicle, makes creation completion explicit and bounds Swift-owned holder scopes without weakening factory idempotence. |
| D-91 | Stable Swift names for exported common enums | Pin `Confirmation`, `AuthProvider` and `SyncTrigger` to Kotlin-matching exact Objective-C and Swift names | Pin the previous module-prefixed names; retain module-derived names | Accepted | No Swift source consumes the legacy names; explicit annotations make the intentional E1-07 rename stable across framework export configuration. |
| D-92 | Fuel Entry row indicators | Add `hasMissedEntries` and `odometerInconsistent` to `FuelEntryListItem` | Parallel indicator projection; per-row full-entry reads | Accepted | Preserves overlapping warning facts in the single reactive list projection, including partial rows whose primary reason is `EndEntryNotFullTank`. |
| D-93 | Live Fuel Entry money resolution | Promote the existing pure `MoneyInput` resolver for shared validator and presentation use | Duplicate presentation arithmetic; invoke full validation with synthetic facts | Accepted | Keeps one R-2 implementation; typing errors clear only the derived field and remain silent until save. |
| D-94 | Fuel Entry form defaults | Compose exact clock time, Vehicle odometer and supported locale currency in `AppGraph` until E1-10 supplies settings | Add a defaults repository query; implement E1-10 early | Accepted | Preserves feature isolation and one-story scope while making the temporary currency source explicit and replaceable. |
| D-95 | Pre-E3-03 Fuel sync-status staging | Publish constant `SyncStatus.Idle` from the Fuel Entry list until E3-03 | Provisional Fuel controller; pull E3-03 forward | Accepted | Extends the staged exception explicitly without creating a second synchronization authority; E3-03 closes D-88 and D-95. |
| D-96 | Fuel Entry calendar-day conversion | Convert and format selected days in an injected device time zone, preserving untouched `now` | UTC-midnight display; preserve prior local time-of-day | Accepted | Keeps the F-3 fast path on the user's calendar day while retaining deterministic tests and the existing epoch-millisecond ABI. |
| D-97 | Fuel presentation iOS export | Export `:feature:fuel`, hide non-allowlisted declarations and pin existing presentation names exactly | Retain wrappers in `:shared`; export the feature without refinement | Accepted | Moves ownership to the feature without leaking domain/data APIs or renaming/changing any existing Objective-C declaration; the golden records the compiler's ordering-only relocation. |
| D-98 | Fuel form save completion | Publish a Kotlin-only completion flow from the Fuel Entry form holder | Infer completion from `isSaving` and `message`; add a saved field to `FuelEntryFormUiState` | Accepted | Provides an exact success event for Android navigation without racing conflated state or changing the Swift ABI. |
| D-99 | Fuel money mode-switch derivation | Preserve available money values and immediately re-derive the value outside the selected mode through the D-93 resolver | Clear the value outside the selected mode; keep separate drafts per mode | Accepted | Preserves user-entered context across mode changes while accepting visible one-scale-unit rounding drift that the user can correct before save. |
| D-100 | Walking-skeleton debug diagnostics screen | Move anonymous session start and backup restore controls to a Debug-only diagnostics screen reachable from the navigation bar; D-74 keychain XCUITest drives that route | Delete walking skeleton UI; keep controls on production root | Accepted | Keeps E0-07 walking-skeleton acceptance evidence and Keychain persistence tests executable without shipping scaffolding in Release builds. |
| D-101 | iOS deployment target and ObservableObject lifecycle | Set `IPHONEOS_DEPLOYMENT_TARGET = 16.0`, `@MainActor ObservableObject` screen models, single observation task in `init`, cancellation in `deinit`, keyed release call | iOS 17 with `@Observable` macro; direct view collection | Accepted | Retains iOS 16 device compatibility, clean task cancellation in `deinit`, and strictly enforces D-90 / D-91 holder scope bounding. |
| D-102 | iOS test targets in CI | Dedicated `carAppTests` unit test target + extended `carAppUITests`, both run in protected `ios-simulator-build` CI job | Single test target; new CI job | Accepted | Separates fast Swift unit tests from simulator UI tests while keeping protected CI check names strictly unchanged. |
| D-103 | iOS NavigationStack and sheet presentation | Root `NavigationStack` with typed route for list -> detail; modal `.sheet` presentation for Vehicle Form and Fuel Entry Form | Push all screens on NavigationStack; UIKit coordinator | Accepted | Follows iOS Human Interface Guidelines for data entry dialogs and typed destination safety. |
| D-104 | Scaled value formatting parity on iOS | Swift pure integer formatting with strict scale constants (1000, 1000, 100, 100) and comma/dot parsing | Double or Decimal with NumberFormatter | Accepted | Guarantees exact numerical and visual parity with Android without IEEE-754 floating-point drift. |
| D-105 | Continuous agent progress handoff | Maintain the story handoff from intake through completion, checkpoint every material state change and before yielding unfinished work | Checkpoint only at TDD phase boundaries; update only at completion and rely on chat history | Accepted | Makes agent replacement recoverable from versioned repository state while keeping the project log append-only and completion-focused. |
| D-106 | Settings default bootstrap ownership | Keep `SqlDelightSettingsRepository` self-healing and add a one-shot, best-effort `AppGraph` bootstrap whose scope is cancelled before the database closes | Explicit suspending host initializer; create defaults in `DatabaseFactory` | Accepted | Satisfies literal first-launch creation without making startup depend on it or moving locale-derived defaults into `:core:database`; repository access remains the recovery authority. Cancellation precedes database close, but the pre-write gate is still check-then-act and retains the residual D-89 window owned outside E1-10. |
| D-107 | Fuel Entry form settings snapshot | A new form consumes only the first persisted currency, with locale fallback and explicit-edit protection | Graph-wide settings `StateFlow`; suspending read before holder construction | Accepted | Gives each new form one stable persisted default without changing an already-open form, an existing entry or the synchronous Swift-facing factory. |
| D-108 | Native locale-provider ownership | Inject native Android and iOS `LocaleProvider` adapters at host composition boundaries and keep the production Firebase provider overload explicit | Own adapters in Firebase wiring source sets; pass a one-time `LocaleInfo` snapshot | Accepted | Keeps platform APIs at host edges, common code provider-free and production dependencies explicit while retaining internal staged defaults for tests. |
| D-109 | Native locale-provider verification topology | Run Android application unit tests canonically; compile the exact composition-owned iOS adapter source into `:shared` `iosTest` and execute it through canonical `iosSimulatorArm64Test` | Export a Swift test facade; move the adapter into a dedicated testable module | Accepted | Executes reachable native locale and currency-code paths and pins the Foundation fraction-digit premise without changing the closed Swift ABI, moving production ownership or adding a second Firebase Apple dependency route; a source-ownership guard prevents silent test drift. |
| D-110 | Outbox entity-type token ownership | Keep explicit contract tokens (`"VEHICLE"`, `"FUEL_ENTRY"`) in production code; add a test-only anchor pinning `EntityType` enum names to the outbox wire values | Extract shared constants now; derive all values from the `:core:sync` `EntityType` | Accepted | Avoids a gated module-boundary change in E1-11; the enum-to-contract anchor makes a future enum rename fail the build; any centralization is deferred to a separate Ready Phase 3 story. |
| D-111 | Explicit UID change opt-in for account adoption | Add optional parameter `allowUidChange: Boolean = false` to `AuthClient.signInWithCredential`; default rejects anonymous UID changes with `AuthError.UidWouldChange`, explicit `true` permits account adoption | Always allow UID change unconditionally; split into separate `adoptAccountWithCredential` API | Accepted | Preserves existing contract callers and safety invariants while unblocking the CONTRACTS.md §11.3 Step 2 collision adoption flow from an anonymous session. |
| D-112 | Android Google credential acquisition | Use Credential Manager 1.6.0 with its Play Services Auth bridge 1.6.0 and Google ID 1.2.0 | Credential Manager 1.7 alpha; legacy Google Sign-In | Accepted | All three pins live only in `gradle/libs.versions.toml`; native Android UI acquires credentials and no credential or token enters shared UI state, analytics, logging or crash reporting. |
| D-113 | iOS native provider acquisition | Use `AuthenticationServices` plus `CryptoKit` for Apple and exact GoogleSignIn-iOS 9.2.0 for Google | GoogleSignIn-iOS 10.0.0; manual Google OAuth; FirebaseUI | Accepted | Apple uses a SHA-256 nonce while retaining the raw nonce for Firebase; GoogleSignIn belongs to the Firebase compatibility set and MUST be revalidated whenever D-65 moves. |
| D-114 | Native-to-shared sign-in handoff | Start provider selection in shared state, then complete through provider-named primitive intents or fail through a closed exported enum | Inject native credential acquirers into shared code; expose a graph-level native credential exchanger | Accepted | `NativeAuthCredential` is not exported; tokens never enter `UiState`, analytics, `Logger` / Kermit or crash reporting, and failure or cancellation never crosses as free text. |
| D-115 | Onboarding navigation topology | Mount the authenticated navigation graph once with the vehicle list as its root and push first-run vehicle creation over it | Rebuild the graph whenever the owner's vehicle count changes; freeze the destination and rebuild the back stack after the first save; route to the vehicle list after the first save | Accepted | Mounted navigation MUST NOT be derived from owner state; first-run creation is identified by its own route, exposes no back or cancellation affordance, and still routes to the created vehicle detail. |
| D-116 | Vehicle list loading semantics | `VehicleListUiState.isLoading` means the vehicle list is not known yet | Publish the refresh flag; add a second state field; derive the distinction separately in each native host | Accepted | Hosts gate F-1 first-run routing on this value; while it is true they MUST cover the mounted UI instead of replacing it, and an ordinary refresh MUST NOT set it. |
| D-117 | Native sign-in interruption recovery | Handle the common host configuration changes in place and abandon an orphaned acquisition through the closed cancellation intent | Rely on configuration handling alone; add a dedicated abandonment intent; expire the busy state on a timeout | Accepted | Abandonment MUST reuse `failSignIn(NativeSignInFailure.CANCELLED)` and MUST NOT key off `isBusy`, which anonymous sign-in also sets; cancellation leaves a retryable state with no user-visible error. |
| D-118 | iOS device signing supply | Keep the developer team out of the repository and supply it from an untracked `iosApp/Local.xcconfig` that the committed `Signing.xcconfig` optionally includes | Pass the team on each build command; commit the team and supersede D-71; defer device signing to the release phase | Accepted | `DEVELOPMENT_TEAM` MUST NOT appear in `iosApp/project.yml` or the generated project; the committed configuration MUST stay a no-op when the local file is absent, which is the state of CI. |
| D-119 | Declared inputs for the repository guards | Declare the committed repository files that `:build-logic:convention:test` reads as task inputs | Mark the task as never up to date; rely on `--rerun-tasks` discipline | Accepted | A guard that reads a repository file MUST have that file declared, so the task cannot report `UP-TO-DATE` after the configuration it guards has changed; `GuardedRepositoryInputsTest` fails when a new read is undeclared. |
| D-120 | Owner-scoped vehicle list resolution | Resolve `VehicleListUiState.isLoading` per owner: publish the transition synchronously with the authentication change, re-subscribe the observation for every owner, and keep the list unknown until that owner publishes a successful result | Pair each emission with the owner read at map time; add an owner field to the state; keep resolving on the first emission of any owner | Accepted | An owner transition MUST reopen the list before any other observer can expose the new session, and MUST clear that owner's list, selection and message; a read failure MUST stay unknown, MUST publish its error and MUST offer a retry that creates a new observation; an ordinary refresh over an already known list MUST NOT reopen it (`D-116`). |
| D-121 | Mandatory first-run vehicle creation | Consume the Android system and predictive back gestures and disable iOS interactive dismissal for first-run creation only | Leave the platform gestures escaping the form; make later creation mandatory too; block the gestures application-wide | Accepted | Supersedes the accepted consequence of `D-115` that the Android system back gesture reaches the empty list. Later creation and every edit route MUST keep the platform behaviour. |
| D-122 | No-account sign-in outcome | Report a device with no account to offer as its own `NativeSignInFailure.NO_ACCOUNT_AVAILABLE` case, mapped to the new `AuthError.NoAccountAvailable` leaf with code `AUTH.NO_ACCOUNT_AVAILABLE` | Keep `UNKNOWN` and the generic message; show an Android-only host message outside the shared taxonomy; reuse `CONFIGURATION` | Accepted | The case MUST mean "this device has no account to offer" and never a provider configuration problem; its host message MUST name the always-available "continue without an account" path; a host without a reliable signal keeps `UNKNOWN` rather than reporting it speculatively. Widens the exported enum, the auth error taxonomy, the Swift ABI and the committed Objective-C golden header. |
| D-123 | Local owner adoption transaction ownership | Own the whole `§11.4` operation in one `DatabaseMutations.adoptLocalOwner` transaction and inject the outbox payload as a pure function over the module's own row types | Build the payloads inside `:core:database`; orchestrate several transactions from `:shared` | Accepted | The injected payload functions MUST be pure and non-suspending, because they run inside the write transaction; the adoption rewrite MUST NOT consume a `localMutationSeq`; a new outbox entity MUST join this function's selection and dependency-group partition rather than gaining a second adoption path. |
| D-124 | Automatic anonymous retry gate | Run the `§11.2` retry when the owner is the sentinel, the auth state is `SignedOut`, no acquisition is in flight, and either this process remembers the explicit "continue without an account" choice or rows are still owned by the sentinel; trigger it from returning connectivity and from the first write committed under the sentinel | Gate on `LOCAL_OWNER` rows alone; gate on the in-memory `SessionPhase.LOCAL` alone; persist a device-local "local start accepted" flag | Accepted | Returning connectivity MUST NOT create an account for a device that has neither the explicit choice nor rows; the retry MUST NOT run while the auth state is `Unknown`; concurrent triggers MUST produce at most one attempt; a future trigger that needs an account without either signal requires its own decision. |
| D-125 | Adoption read gate | Hold every Vehicle read and write until local owner adoption has run for the current owner, and report a failed adoption as a typed `PersistenceError.TransactionFailed` on both read and write paths | Accept the transient empty list and let it correct itself; add an "adoption pending" field to `VehicleListUiState` | Accepted | The gate MUST be a no-op for the `LOCAL_OWNER` sentinel, or the `E2-03` offline start breaks; every Vehicle read path MUST pass through it, or it can publish the empty list that `D-116` turns into mandatory first-run creation; a failure MUST reach the caller as a typed error, MUST NOT be thrown across the gate, MUST NOT cancel the caller and MUST NOT be a silent indefinite wait; `CancellationException` MUST be rethrown; the automatic triggers MUST NOT be able to cancel one another. Supersedes nothing: `D-116`, `D-120` and `D-121` keep their exact meanings. |
| D-126 | Native connectivity-provider ownership | Inject real `ConnectivityObserver` implementations at each host composition boundary - `ConnectivityManager` default-network callbacks on Android, `NWPathMonitor` on iOS - and make the staged default offline rather than online | Implement inside `:wiring:firebase` source sets; defer to `E3-08`; add a connectivity library | Accepted | A host composition boundary MUST inject a real observer; the staged default MUST NOT report online, because a default that claims connectivity is a false fact rather than a conservative one; platform connectivity APIs MUST NOT appear outside the host boundaries, exactly as `D-108` requires for locale. |
| D-127 | Fuel type selector in the MVP UI | Expose a `FuelType` selector over the five MVP values in the Vehicle creation and edit forms on both platforms, defaulting to `GASOLINE` | Rewrite `D-4` in place; keep `D-4` and drop `E1-16` | Accepted | Supersedes only the "no MVP UI selector" clause of `D-4`; everything else in `D-4` stands. The selector MUST offer exactly the five MVP values, `ELECTRIC` and `HYBRID` remaining owned by `E5-01`; the default MUST stay `GASOLINE`; and no schema, migration or Firestore rule change is authorised. |
| D-128 | Account-deletion callable wire contract | Use a 2nd gen `deleteAccount` callable with `targetUid`, explicit caller/target equality, `{ status: "ACCOUNT_DELETED" }` success and closed callable error codes | Infer the target solely from auth; expose a privileged arbitrary-target endpoint | Accepted | Keeps caller mismatch executable, gives the client a stable typed wire contract and prevents this user-facing operation from becoming a general administrative deletion API. |
| D-129 | Firestore Admin deletion primitive | Await Firestore Admin `recursiveDelete` sequentially for each registered owner collection | Hand-written paged batches; one delete call per fetched document | Accepted | Uses the SDK-owned idempotent bulk implementation while preserving the normative `fuelEntries` then `vehicles` ordering and the exact `users/{uid}` boundary. |
| D-130 | Transitive `qs` advisory remediation | Resolve `qs` to patched 6.16.0 in the lockfile within the existing Express and Body Parser ranges | Accept the new residual; force a direct override or downgrade the Functions stack | Accepted | Removes two network-reachable moderate advisories without adding a direct dependency, changing an accepted top-level pin or weakening the repository-wide install-script policy. |
| D-131 | Account-deletion callable runtime bounds | Bound `deleteAccount` to three single-concurrency 256 MiB instances with a 300-second timeout | Leave the callable unbounded and rely on the automatic billing cutoff; extend D-67 App Check enforcement to Cloud Functions now | Accepted | Explicit runtime bounds are the primary cost and workload control; the D-66 cutoff remains a delayed safety net. Cloud Functions App Check and a dedicated least-privilege service account are deliberately deferred. |
| D-132 | Cloud Functions App Check scope | Keep D-67 enforcement on Authentication and Firestore; Cloud Functions callables are not App Check-enforced | Enforce App Check on the deletion callables now; monitor-only first, enforce later | Accepted | Closes the D-131 deferral for the callable surface. D-141 replaces the captured-token proof for orphan cleanup with a server-issued authorization ticket and applies the same posture to its issuer; the D-66 budget remains the development cost safety net. Any future Functions App Check change covers both deletion callables and the ticket issuer together. |
| D-133 | Orphan-cleanup callable wire contract | `deleteOrphanedAnonymousAccount` takes `anonymousIdToken`, returns `{ status: "ORPHANED_ANONYMOUS_ACCOUNT_DELETED" }` and closes errors to `unauthenticated` / `invalid-argument` / `failed-precondition` / `internal` | Reuse the D-128 surface; add `permission-denied` for the permanent-UID guard | Superseded | Superseded by D-141, which replaces `anonymousIdToken` with a server-issued cleanup ticket while retaining the success literal and closed error-code set. |
| D-134 | Anonymous cleanup trigger eligibility | A deleted Auth user is eligible when `providerData` is empty; any provider entry or a missing UID is skipped with a redacted log | Cover empty or password-only records; delegate every deletion and rely on idempotency | Accepted | Matches the Firebase representation of an unlinked anonymous account; linked and phone-only users cannot be purged by the automatic-cleanup trigger; trigger/callable overlap stays harmless because both paths are idempotent. |
| D-135 | Orphan-cleanup callable runtime bounds | Bound `deleteOrphanedAnonymousAccount` to two 256 MiB instances with a 60-second timeout and default concurrency in `europe-west1` | Reuse the exact D-131 bounds; leave provider defaults | Accepted | Keeps explicit bounds as the primary workload control for the shorter authorization-lookup-and-deletion operation; must be re-evaluated before the deletion registry gains Storage prefixes or larger collections. |
| D-136 | Sole-1st-gen allowlist enforcement | Keep the Functions suite as behavioral owner and add a `FunctionGenerationContract` to `contractCheck` with a failing fixture | Keep the allowlist only in `functions/`; report it from `contractCheck` as informational | Accepted | Makes the TD-01 guard visible to the CI contract job and to the local Gradle contract command without changing the TD-01 migration surface; closing TD-01 updates both checks in one change. |
| D-137 | Anonymous cleanup trigger region | Pin `onAnonymousUserDeleted` explicitly to `europe-west1` | Leave unconfigured on provider default (`us-central1`) | Accepted | Colocates the trigger execution with Cloud Firestore and the project's other backend functions; eliminates cross-region traffic and preserves the regional deployment invariant of D-13 and D-22. |
| D-138 | Anonymous cleanup trigger runtime bounds and retry policy | Bound `onAnonymousUserDeleted` to two 256 MiB instances with a 60-second timeout and set `failurePolicy: true` (`eventTrigger.retry = true`) | Provider defaults with no failure policy; runtime bounds without retry | Accepted | Explicit bounds prevent runaway execution while platform retries ensure transient Firestore deletion failures are redelivered until completion, preserving the deletion guarantee. |
| D-139 | Expired anonymous token retry convergence | Permit well-formed expired anonymous ID token on retry if and only if Auth user is already deleted (`auth/user-not-found`) | Server-side Firestore intent document; reorder deletion | Superseded | Superseded by `D-140`; expired token retry convergence cryptographically verifies Google RS256 signatures with an iat window for active Auth deletion and Firestore cleanup. |
| D-140 | Expired anonymous token retry convergence via RS256 signature verification | Cryptographically verify Google RS256 signature (with iat window) of expired anonymous ID token to converge deletion of active Auth user and Firestore data | Pre-authorization lease in Step 1; reorder Auth deletion to Step 1; unverified expired token decoding | Superseded | Superseded by D-141 after security review proved the implementation accepted foreign-project tokens and an attacker-controlled `uid` claim; the hand-written expired-token verifier is removed. |
| D-141 | Server-issued orphan-cleanup authorization | Issue a 256-bit opaque cleanup ticket from an authenticated anonymous session before account switching; store only its SHA-256 digest and server-bound UID in an inaccessible 30-day TTL record, then require that ticket from the permanent caller | Correct the expired-token verifier in place; retain D-139; delete Auth before account switching | Accepted | Supersedes D-133 and D-140. `issueOrphanCleanupTicket` and `deleteOrphanedAnonymousAccount` are bounded 2nd gen callables without Functions App Check under the existing D-132 posture; verified caller context, unguessable single-purpose authorization, default-denied storage, completion-last idempotency and D-138 trigger retries preserve security and convergence without any JWT verification fallback. Amended by D-142, which adds ticket-consumption anonymity revalidation, and by D-143, which erases ticket authorizations on account deletion. |
| D-142 | Ticket-consumption anonymity revalidation | Before any destructive stage, `deleteOrphanedAnonymousAccount` resolves the bound UID's current Auth record via Admin `getUser` and rejects it with `failed-precondition` unless it is missing or still satisfies the D-134 empty-`providerData` predicate; other lookup failures map to redacted `internal` | Re-check only `sign_in_provider`; accept the issuance-time anonymity for the ticket lifetime; purge the ticket when its bound account links | Accepted | Amends the D-141/ADR-0142 threat model: the ticket cannot select a UID, but the bound UID's account may cease to be anonymous between issuance and consumption, and a stale durable client marker could otherwise delete a permanent account. The single shared D-134 predicate keeps one eligibility definition across the trigger and the callable; missing-account convergence preserves completion-last retries. |
| D-143 | Account-deletion erasure of orphan-cleanup authorizations | Account deletion purges every `orphanCleanupTickets` record bound to the deleted UID after registered remote data and before Auth deletion; internal server-only collections are declared in a separate CONTRACTS §16 registry excluded from the D-63 user-data traversal and pinned by an internal parity test | Accept TTL-only retention of `anonymousUid` after account deletion, which is a 30-day expiration horizon followed by provider-managed asynchronous cleanup rather than a retention bound; duplicate firebase-tools as a functions devDependency | Accepted | The purge owns the erasure of every authorization visible to it when it runs, so the 30-day TTL is only a provider-managed eventual-cleanup fallback for abandoned or completed authorizations, with a non-hard-bounded deletion delay. It is **not** an unconditional zero-retention guarantee: the concurrent issuance documented in ADR-0150 can write after the purge, and closing that race belongs to `D-149` and `E3-15`. The internal registry keeps the client-facing remote schema closed while making every server-only collection explicit; an undeclared internal collection or a D-63 overlap fails the contract tests. |
| D-144 | Anonymous reminder persistence | Persist the zero-based last-shown reminder index next to its anonymous UID in a dedicated single-row `anonymous_reminder` table, added by schema version 2 through the committed `1.sqm` migration | A new `user_settings` column; a platform key-value store injected through `AppGraphDependencies` | Accepted | The schedule position is not a user preference and is only meaningful next to the identity that produced it, so a stored position belonging to another anonymous UID reads as absent and restarts the schedule. The migration is additive, so no existing table, column or row can be lost, and `docs/TECHNICAL_PLAN.md §6` still requires a populated previous-version migration test for every later bump. The table is device-local: never synchronized, never enqueued and absent from the closed remote schema of `docs/CONTRACTS.md §16`. |
| D-145 | Anonymous reminder presentation channel | Expose `SessionUiState.anonymousReminderIndex: Int?` and `dismissAnonymousReminder()`, independent of `message` and `clearMessage()` | Reuse `UiMessage` with an `INFO` code on the existing single message channel; a separate reminder state holder with its own `UiState` | Accepted | The message channel is already owned by authentication errors, so a foreground evaluation would silently replace an unread error and a later error would silently remove the notice. The typed index also reaches the hosts, so each of the four notices carries its own escalating body in English and Spanish, naming only the providers that platform offers. The exported `SessionUiState` and the committed Objective-C golden header change together, and the golden diff is the review signal. |
| D-146 | Anonymous reminder evaluation trigger | Each host calls `SessionStateHolder.evaluateAnonymousReminder()` on launch and foreground return, from the Android `ON_START` event and the iOS active scene phase | A new `AppLifecycleObserver` member of `AppGraphDependencies`; evaluation on every auth-state emission | Accepted | Keeps the canonical `AppGraphDependencies` parameter order of `docs/CONTRACTS.md §11.6`, its `AppProviders` mirror, `testAppGraphDependencies` and both composition roots unchanged for one lifecycle event that the intent mechanism of §14 already covers. No scheduler, alarm, background task or operating-system notification is introduced, and the intent stays a no-op for `LOCAL_OWNER`, signed-out and permanent sessions. |
| D-147 | Deferred launch evaluation | Mark an evaluation blocked by `AuthState.Unknown` as pending and let the existing auth-state collector complete it exactly once when the state resolves to an anonymous `SignedIn` | Evaluate on every anonymous `SignedIn` emission; leave the notice to the next foreground return | Accepted | `docs/CONTRACTS.md §11.1` separates *not yet determined* from *signed out*, and only the former defers. The deferral is one-shot, any other resolution consumes it without running it, and a resolution nobody asked for evaluates nothing, so a restored session never becomes a trigger and `D-146` keeps its single entry point. No scheduler, alarm, background task or observer beyond the host intents is introduced, and `close()` drops a pending request. |
| D-148 | Admin verification at ticket issuance | `issueOrphanCleanupTicket` resolves the caller's current Firebase Auth record through the Admin SDK before generating or persisting anything, and issues only when that record exists, is known to be enabled — an explicit `disabled === false` state, failing closed when the state is unavailable — and still satisfies the shared `D-134` empty-`providerData` predicate | Require a fresh `auth_time` instead of an Admin read; rely on the `D-142` consumption-time revalidation alone | Accepted | Callable token verification performs no revocation or current-user check, so an anonymous claim stays true for the rest of a token's lifetime after linking, disabling or deletion. An eligibility rejection is `failed-precondition` and creates no authorization; an Admin lookup failure is `internal` with the redacted `AUTH_USER` stage. Neither leaks the UID, token, payload or raw provider failure. The predicate is the single shared `D-134` definition, so the trigger, the consumption callable and the issuer cannot drift. |
| D-149 | Ticket issuance and account deletion race | **Undecided, and no recommendation is on the table.** No option satisfies the synchronous crash-safe erasure property without a serialization proof, so the owner chooses between a residual window whose cleanup today is only the provider-managed asynchronous TTL (options A and B) and a transactional serialization point whose marker retention design is still open (option C), each with the proof obligations of ADR-0150 | A second `orphanCleanupTickets` purge pass after Auth deletion alone (partial synchronous cleanup of the authorizations already visible when it runs — neither crash-safe erasure nor the global convergence property: the issuer's write can land after the pass and the issuer can crash before any compensating step); disabling the Auth user first, which narrows the window but removes no record and adds no cleanup of its own; an internal `deletedUids` marker collection whose never-expiring marker retains a UID-correlatable key for every deleted account, which conflicts with `D-143` | Pending | `D-148` cannot close this on its own: the `§11.5` order purges authorizations before deleting the Auth user, so an issuance whose eligibility check passes before the purge and whose write lands after it survives a successful deletion. A post-write read-back or a later purge pass is not atomic with either the deletion or the authorization creation — the owner's counterexample in ADR-0150 shows a record outliving success, after which only the provider's asynchronous TTL cleanup removes it, eventually and with no proven maximum — so no option MAY be described as guaranteeing "zero UID-bound authorizations when deletion returns success" without a complete crash-safe serialization proof, and no option MAY claim a bounded residual window on the strength of the existing 30-day TTL. A provable maximum retention period would require an additional deterministic cleanup mechanism selected by the owner and is not designed here. Needed by `E3-15`; see ADR-0150 for the interleaving analysis, the convergence-versus-erasure distinction, the option C retention problem and the proof obligations. |

## Library Review Matrix

| Area | Library / Tool | Alternatives | Status | Notes |
|------|----------------|--------------|--------|-------|
| Coroutines and streams | `kotlinx.coroutines` + Flow | callbacks, Rx | Accepted | Required for KMP async and state streams. |
| Serialization | `kotlinx.serialization-json` | Moshi, manual JSON | Accepted | Required for outbox payloads and remote DTOs. |
| Date/time | `kotlinx-datetime` | platform date APIs only | Accepted | UTC instants only in persistence. The exact `Instant` package is pinned in `docs/versions-matrix.md`. |
| Logging | Kermit behind `Logger` | Napier, custom sinks | Accepted (D-15) | Abstraction mandatory; implementation swappable. Does not replace `AnalyticsTracker` or `CrashReporter`. |
| Crash reporting | Firebase Crashlytics behind `CrashReporter` | Sentry, none | Accepted (D-21) | Phase 4. |
| Flow testing | Turbine | manual collection | Accepted (D-17) | Validate against the pinned coroutines version. |
| Test assertions | `kotlin.test` | Kotest | Accepted | Keep tests simple for agent predictability. |
| Test doubles | Hand-written fakes | MockK, Mockative | Accepted | Fakes are preferred for domain and sync. |
| Firestore rules tests | Node 22.22.3, Firebase CLI 15.28.1, Firebase JS 12.18.0, `@firebase/rules-unit-testing` 5.0.1, `node:test` | Direct REST harness, Kotlin/GitLive integration tests, Jest/Mocha/Vitest | Accepted (D-46) | Official emulator auth mocking; exact Node-only dependency versions live in npm manifests. |
| Firebase Apple SDK | 11.8.0, matched exactly to GitLive 2.6.0 Apple bindings | Firebase Apple 12.18.0; Firebase Apple 11.15.0 | Accepted (D-65) | Direct iOS integration must not mix the GitLive cinterop bindings with a different native Firebase Apple release. |
| Android Google credential acquisition | Credential Manager 1.6.0, Play Services Auth bridge 1.6.0, Google ID 1.2.0 | Credential Manager 1.7 alpha; legacy Google Sign-In | Accepted (D-112) | Stable native host stack; all pins live only in the Gradle version catalog and move together. |
| iOS Google credential acquisition | GoogleSignIn-iOS 9.2.0 exact SwiftPM pin | GoogleSignIn-iOS 10.0.0; manual Google OAuth; FirebaseUI | Accepted (D-113) | Part of the Firebase compatibility set; revalidate the complete SwiftPM graph whenever D-65 moves. |
| iOS Apple credential acquisition | `AuthenticationServices` plus `CryptoKit` | Third-party wrapper; FirebaseUI | Accepted (D-113) | First-party frameworks only; send Apple a SHA-256 nonce and retain the raw nonce ephemerally for Firebase exchange. |
| Cloud billing cutoff | Node.js 22, Firebase Functions 7.3.2, Firebase Admin 14.3.0, `@google-cloud/billing` 6.0.0, TypeScript 7.0.2 | Python; Node.js 24 in a second codebase; temporary Node.js 24 downgrade; Functions Framework; forced transitive overrides | Accepted (D-66, D-68) | One official Functions package supports the D-66 2nd gen Pub/Sub trigger and the D-63 temporary 1st gen Auth trigger. Runtime debt and the expiring moderate advisory review are tracked by TD-01. |
| Cloud runtime CI identity | GitHub OIDC, Workload Identity Federation, one-permission custom role, SHA-pinned Google actions | Service-account key; local-config-only check | Accepted (D-66) | Provider admission and the service-account IAM binding both restrict the immutable repository identity; CI can only read the deployed function runtime. |
| Firebase App Check | App Attest on iOS, Play Integrity on Android, debug providers in local/CI builds only | No App Check; monitoring-only integration | Accepted (D-67) | Authentication and Firestore enforcement is required before any build leaves local development. |
| Coverage | Kover | JaCoCo, none | Accepted (D-18) | Thresholds enforced in CI. |
| Android background work | WorkManager | foreground-only sync | Accepted for Phase 3 | Trigger only: it calls `SyncController.requestSync(reason)` and carries no scheduling policy. |
| iOS background work | BGTaskScheduler | foreground-only sync | Accepted for Phase 3 | Same constraint; a single task identifier. |
| Connectivity | `ConnectivityObserver` behind a common interface | Ktor-only detection, platform-only direct usage | Accepted | Injected, not `expect`/`actual` in public API. |
| Localization | Native Android/iOS resources | shared resource library | Accepted (D-20) | Native resources because UI is native. |
| Architecture checks | Konsist + custom Gradle check | custom only, dependency-analysis plugin | Accepted (D-16) | See D-16. |
| Charts | None for MVP | Vico, Swift Charts | Rejected for MVP | Advanced charts are out of scope. |
| Image loading | Coil | SDWebImage, platform-native | Deferred (D-12) | Only if a story requires it. |
| HTTP client | Ktor | Retrofit, URLSession | Deferred (D-11) | Only with an approved API implementation story. |
| Secrets/config | Platform Firebase config files and CI secrets | committed secrets | Accepted | See `docs/SECURITY.md` for the allowlist and denylist. |
| Static analysis | ktlint + detekt + Android lint | none, Spotless-only | Accepted | Config files committed in Phase 0. Baseline suppression files are FORBIDDEN. |
| Local database | SQLDelight 2.3.2 | Room 3 KMP, Realm | Accepted (D-36) | SQL is the schema source; generated database types stay owned by `:core:database`. |
| SQLDelight AndroidX driver | `com.eygraber:sqldelight-androidx-driver` 0.2.1 | Official platform drivers, repository-owned adapter | Accepted (D-36) | Wraps `androidx.sqlite:sqlite-bundled`; restricted to `:core:database`, with async generation and multiplatform execution tests. |

## Ktor Decision

Ktor is not part of the initial MVP dependency set while Firebase is the selected database backend.

The architecture must still support a future migration path:

```text
RemoteSyncSource
  -> FirebaseRemoteSyncSource        MVP implementation
  -> HttpApiRemoteSyncSource         Future Ktor implementation
  -> SupabaseRemoteSyncSource        Future option
  -> AwsRemoteSyncSource             Future option
```

Adding Ktor requires a new implementation story and an ADR update. Until then, agents MUST NOT add Ktor dependencies.

## Firebase Decoupling Rule

Firebase is an implementation detail. These abstractions are mandatory:

- `AuthClient`
- `TokenProvider`
- `RemoteSyncSource`
- `AnalyticsTracker`
- `CrashReporter`

Firebase SDK, GitLive and native Firebase types are allowed only inside integration and wiring modules.

## Decisions Awaiting Owner Confirmation

| ID | Area | Recommendation | Needed by | Consequence if unresolved |
|----|------|----------------|-----------|---------------------------|
| D-149 | Ticket issuance and account deletion race | **None — this decision is `Pending`.** No recommendation, default or pre-selection is offered. Option A (a second purge pass) is partial synchronous cleanup: it removes only the authorizations already visible when it ran, which is neither crash-safe erasure nor the global eventual-convergence property. Option B (disabling the Auth user first) narrows the set of interleavings that can produce a surviving record without removing anything or adding cleanup of its own. A record that escapes either is removed only by the pre-existing provider-managed Firestore TTL, eventually and with no proven maximum, per the ADR-0150 counterexample. Option C (an internal `deletedUids` marker collection read inside the issuance transaction) is the only option whose transaction can provide synchronous crash-safe erasure, but its proposed never-expiring marker is a separate indefinite UID-linked retention problem that conflicts with `D-143` and MUST be resolved before the option satisfies the project's erasure posture. The owner chooses; the accepted option discharges the proof obligations of ADR-0150, and a provable maximum retention period would require an additional deterministic cleanup mechanism the owner also selects. | `E3-15` | The `D-143` erasure guarantee stays conditional: in the interleaving analysed in ADR-0150, one UID-bound authorization record can outlive a successful account deletion, after which it is removed only by provider-managed asynchronous TTL cleanup following its 30-day expiration horizon, with no guaranteed maximum. `E3-15` cannot start. If the owner defers instead of deciding, the residual risk MUST be recorded in `docs/SECURITY.md`. |

A story named in `Needed by` MUST NOT start until its row is resolved. This section MUST keep the columns `ID`, `Area`, `Recommendation`, `Needed by` and `Consequence if unresolved`.
