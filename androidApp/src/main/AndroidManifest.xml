<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      carApp MVP. No platform backup or settings-sync surface (docs/BACKLOG.md E0-01 AC#7,
      docs/SPECIFICATION.md out-of-scope: platform settings sync / backup through
      Google Play services / Android backup / iCloud is explicitly excluded).
    -->
    <application
        android:name=".CarAppApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
